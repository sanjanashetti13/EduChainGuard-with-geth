import React from "react";

import { CinematicHeroSection } from "components/ui/ethereal";

/** Isolated cinematic hero — also available via `/`. */
export default function EtherealDemo() {
  return <CinematicHeroSection />;
}
