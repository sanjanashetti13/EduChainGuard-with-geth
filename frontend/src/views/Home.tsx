import ScrollHero from "components/ui/ethereal";

/** Optional full-bleed 3D scroll scene (no hero navbar). Reach via `/explore`. */
export default function ExploreScroll() {
  return <ScrollHero hideNav logo="EduChainGuard" menuItems={[]} />;
}
